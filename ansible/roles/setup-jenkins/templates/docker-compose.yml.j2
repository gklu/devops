version: '3.1'
 
services:
 
  Jenkins:
    image: milcloud/jenkins
    ports:
      - 80:8080
      - 50000:50000
    secrets:
      - jenkins-user
      - jenkins-password
    restart: always
    
secrets:
  jenkins-user:
    external: true
  jenkins-password:
    external: true
