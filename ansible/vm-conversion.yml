---
- name: Perform vm conversion
  hosts: "{{ host  }}"
  become: yes
  # become_user: root
  # become_method: sudo
  vars:
    - download_url: https://s3.amazonaws.com/s/downloads/
    - url: http://214.74.65.210/
  roles:
    - configure_cloudstack_credentials
    - vm-conversion

    
